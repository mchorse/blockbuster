# Config strings
blockbuster.config:
    title: Blockbuster

    general:
        title: General
        tooltip: General config options of Blockbuster mod

        show_first_time_modal: Show first time modal
        extra_wubs: Extra Wubs
        auto_refresh_models: Refresh models and skins
        debug_playback_ticks: Debug ticks
        green_screen_sky: Chroma sky
        green_screen_sky_color: Chroma sky color
        url_skins_sync_download: Synchronous URL downloads
        add_utility_blocks: Add utility blocks
        bb_gun_sync_distance: BB gun sync. distance
    
    model_block:
        title: Model block
        tooltip: Model block related config options
        
        model_block_disable_rendering: Disable model block rendering
        model_block_disable_item_rendering: Disable item rendering
        model_block_missing_name_rendering: Render missing model path
        model_block_debug_rendering_f1: Render model block debug in F1 mode
        restore: Restore model blocks
        reset_on_playback: Reset on playback
    
    recording:
        title: Recording
        tooltip: Here is everything related to player recording system

        recording_countdown: Recording countdown
        record_unload_time: Automatic record unload time
        record_unload: Automatic record unloading
        record_sync_rate: Record tick sync rate
        record_attack_on_swipe: Attack on swipe
        record_commands: Record commands
        record_chat_prefix: Chat action prefix
        record_pause_preview: Better preview

    scenes:
        title: Scenes
        tooltip: Here is everything related to scenes

        save_update: Update scene after saving

    actor:
        title: Actor
        tooltip: Actor entity configuration option (not related to fake player)

        actor_fall_damage: Actor fall damage
        actor_tracking_range: Actor tracking range
        actor_rendering_range: Actor rendering range
        actor_always_render: Always render actor
        actor_always_render_names: Always render actor names
        actor_swish_swipe: Swipe swish
        actor_y: Fix Y motion
        actor_disable_riding: Disable riding
        actor_playback_body_yaw: Playback body yaw

    damage_control:
        title: Damage control
        tooltip: Damage control is a system which puts back blocks after an actor got recorded or scene finished playback
        
        damage_control: Damage control
        damage_control_distance: Damage distance
        damage_control_message: Damage control message

    model_folders:
        title: Model Folders
        tooltip: Add additional model folders

        path: Additional Folder

    snowstorm:
        title: Snowstorm
        tooltip: Snowstorm is a particle system that allows create pretty sophisticated particles, here you can find global options related to its particle system

        depth_sorting: Particle depth sorting

    audio:
        title: Audio
        tooltip: Options for in-game audio sync feature

        waveform_visible: Audio bars visible
        waveform_density: Waveform density
        waveform_width: Audio bar width
        waveform_height: Audio bar height
        waveform_filename: Show audio bar's filename
        waveform_time: Show audio bar's playback time
        audio_sync: Sync audio between server and client

    onion_skin:
        title: Onion skin

        seq_prev: Previous morphs
        seq_prev_color: Previous morph's color
        seq_next: Next morphs
        seq_next_color: Next morph's color
        seq_loop_color: Offset marker

    immersive_editor:
        title: Immersive editor

        model_block: Model block
        record_editor: Record editor
